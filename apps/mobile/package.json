{
  "name": "@canvasos/mobile",
  "version": "1.0.0",
  "private": true,
  "description": "CanvasOS mobile app (Capacitor 6)",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter @canvasos/web build && cap copy && cap sync",
    "dev:android": "cap run android",
    "dev:ios": "cap run ios",
    "clean": "rm -rf android ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
